import { LoginFacebookKitEmailDirective } from './login-facebook-kit-email.directive';

describe('LoginFacebookKitEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginFacebookKitEmailDirective();
    expect(directive).toBeTruthy();
  });
});
