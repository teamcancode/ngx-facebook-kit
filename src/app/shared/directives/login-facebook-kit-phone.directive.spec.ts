import { LoginFacebookKitPhoneDirective } from './login-facebook-kit-phone.directive';

describe('LoginFacebookKitPhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginFacebookKitPhoneDirective();
    expect(directive).toBeTruthy();
  });
});
